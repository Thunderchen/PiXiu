sudo: required
dist: trusty
language: cpp
addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake
      - cmake-data
compiler:
  - gcc
  - clang
script: cmake . && make && ./PiXiuPlus